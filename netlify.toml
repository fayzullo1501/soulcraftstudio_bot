[build]
  command = "npm install && npm run build"
  publish = "SOULCRAFTSTUDIO_BOT"
  timeout = 900 # Увеличьте это значение (в секундах) по желанию.

[context.production]
  command = "npm install --only=production && npm run start"